# Values for a standalone llm-service install (advanced / debugging).
# Prefer: make deploy LLM_MODE=local HF_TOKEN=...
#
# Requires OpenShift AI / KServe. Service URL:
#   http://deepseek-r1-distill-qwen-1-5b-vllm/v1
# Llama Stack (not the API) should consume that URL.

device: gpu
rawDeploymentMode: true

secret:
  enabled: true
  hf_token: ""

models:
  deepseek-r1-distill-qwen-1-5b:
    enabled: true
    id: deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B
    device: gpu
