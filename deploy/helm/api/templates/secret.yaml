apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "api.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_DSN: {{ include "api.postgresDSN" . | quote }}
  LLAMA_STACK_BASE_URL: {{ .Values.llamastack.url | quote }}
