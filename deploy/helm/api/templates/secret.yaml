apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "api.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_DSN: {{ include "api.postgresDSN" . | quote }}
  OPENAI_API_KEY: {{ .Values.llm.apiKey | quote }}
