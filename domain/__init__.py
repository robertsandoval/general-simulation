"""Domain packages — adapters, solvers, and other domain-specific code.

Each subdirectory is one domain (e.g. ``aviation``, ``shipping``).
Which domains are loaded is controlled by ``Settings.enabled_domains``.
"""
